---
title: "LAD-Drive: Bridging Language and Trajectory with Action-Aware Diffusion Transformers"
title_zh: LAD-Drive：利用动作感知扩散 Transformer 桥接语言与轨迹
authors: "Fabian Schmidt, Karol Fedurko, Markus Enzweiler, Abhinav Valada"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02035v1"
tags: ["query:eco-ai"]
score: 8.0
evidence: 用于自动驾驶轨迹规划的生成式框架
tldr: 针对自动驾驶中多模态大模型难以将离散语义转化为连续轨迹的问题，本文提出LAD-Drive框架。该框架通过解耦高层意图与底层规划，利用动作解码器推断概率元动作分布以保留导航不确定性，并结合动作感知扩散解码器，通过截断去噪过程将运动锚点细化为安全、符合动力学的轨迹。在LangAuto基准测试中，该方法显著提升了驾驶分数并减少了碰撞。
selection_source: fresh_fetch
motivation: 现有方法在将语义知识转化为轨迹时，受限于单模态规划头或独热编码动作，难以处理复杂的导航不确定性。
method: 提出一种生成式框架，通过概率元动作分布建立显式信念状态，并利用动作感知扩散变压器进行轨迹细化。
result: "在LangAuto基准测试中达到SOTA水平，驾驶分数提升高达59%，且大幅降低了路线偏离和碰撞率。"
conclusion: LAD-Drive通过结构化解耦意图与规划，有效桥接了语言指令与连续驾驶轨迹，提升了自动驾驶的安全性与鲁棒性。
---

## 摘要
尽管多模态大语言模型（MLLMs）为自动驾驶提供了先进的推理能力，但将其离散的语义知识转化为连续轨迹仍然是一个根本性的挑战。现有方法通常依赖于单模态规划头，这在本质上限制了其表示多模态驾驶行为的能力。此外，大多数生成式方法通常以 one-hot 编码动作为条件，丢弃了对复杂场景至关重要的细微导航不确定性。为了解决这些局限性，我们提出了 LAD-Drive，这是一个在结构上将高层意图与底层空间规划解耦的生成式框架。LAD-Drive 采用动作解码器来推断概率元动作分布，建立了一个显式的置信状态，保留了 one-hot 编码通常会丢失的细微意图。该分布与车辆的运动学状态融合，作为动作感知扩散解码器的条件，该解码器利用截断去噪过程将学习到的运动锚点细化为安全且符合运动学可行性的轨迹。在 LangAuto 基准测试上的广泛评估表明，LAD-Drive 取得了最先进的结果，在驾驶评分（Driving Score）上比竞争基准模型高出多达 59%，同时显著减少了路线偏离和碰撞。我们将在 https://github.com/iis-esslingen/lad-drive 公开发布代码和模型。

## Abstract
While multimodal large language models (MLLMs) provide advanced reasoning for autonomous driving, translating their discrete semantic knowledge into continuous trajectories remains a fundamental challenge. Existing methods often rely on unimodal planning heads that inherently limit their ability to represent multimodal driving behavior. Furthermore, most generative approaches frequently condition on one-hot encoded actions, discarding the nuanced navigational uncertainty critical for complex scenarios. To resolve these limitations, we introduce LAD-Drive, a generative framework that structurally disentangles high-level intention from low-level spatial planning. LAD-Drive employs an action decoder to infer a probabilistic meta-action distribution, establishing an explicit belief state that preserves the nuanced intent typically lost by one-hot encodings. This distribution, fused with the vehicle's kinematic state, conditions an action-aware diffusion decoder that utilizes a truncated denoising process to refine learned motion anchors into safe, kinematically feasible trajectories. Extensive evaluations on the LangAuto benchmark demonstrate that LAD-Drive achieves state-of-the-art results, outperforming competitive baselines by up to 59% in Driving Score while significantly reducing route deviations and collisions. We will publicly release the code and models on https://github.com/iis-esslingen/lad-drive.

---

## 论文详细总结（自动生成）

以下是对论文《LAD-Drive: Bridging Language and Trajectory with Action-Aware Diffusion Transformers》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何有效地将多模态大语言模型（MLLM）生成的离散语义推理知识转化为自动驾驶所需的连续、平滑且安全的运动轨迹。
*   **研究背景**：
    *   **模态不匹配**：MLLM 擅长离散 Token 推理，但直接预测坐标点容易产生物理不可行或不稳定的输出。
    *   **回归局限性**：传统回归头（L1/L2 损失）在处理多模态驾驶行为（如左转或右转避障）时会产生“模式平均”效应，导致预测轨迹失效。
    *   **意图丢失**：现有生成式方法多采用独热编码（One-hot）动作作为条件，忽略了复杂场景中导航意图的概率不确定性（Belief State）。

### 2. 方法论
LAD-Drive 提出了一个将**高层语义意图**与**底层空间规划**在结构上解耦的框架，包含三个核心模块：
*   **语言模块 (AD-MLLM)**：基于 LLaVA-v1.5，将多视角图像、LiDAR 点云和导航指令融合为上下文隐藏状态。
*   **动作解码器 (Action Decoder)**：
    *   从 LLM 隐藏状态中推断离散元动作（如左转、换道等）的**概率分布**。
    *   该分布作为显式的“置信状态”，保留了导航中的细微不确定性。
*   **动作感知扩散解码器 (Diffusion Decoder)**：
    *   **特征瓶颈 (Feature Bottleneck)**：通过 MLP 将 4096 维的 LLM 状态压缩至 512 维，过滤掉与规划无关的语义噪声。
    *   **状态-意图融合**：将车辆运动学状态（速度、偏航角）与动作概率分布拼接，形成联合条件。
    *   **截断去噪过程**：使用 K-means 聚类生成的运动锚点（Anchors）作为先验，仅需 2 步去噪即可细化出符合动力学的多模态轨迹。
*   **训练策略**：采用两阶段训练，先进行空间接地（Spatial Grounding）训练，再进行语义对齐（Semantic Alignment）训练。

### 3. 实验设计
*   **数据集与基准**：在 **CARLA 模拟器**的 **LangAuto 基准测试**上进行评估。该基准包含 Tiny、Short、Long 三种不同长度的路线。
*   **对比方法**：包括 LMDrive (CVPR'24)、AD-H、BEVDriver (IROS'25)、SToRM、VLDrive (ICCV'25)、AdaDrive (ICCV'25) 等最先进的 LLM 驱动的自动驾驶模型。
*   **评估指标**：驾驶分数 (DS)、路线完成率 (RC)、违规分数 (IS) 以及各类具体违规统计（碰撞、违章等）。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA L40s GPU**。
*   **训练细节**：
    *   全局 Batch Size 为 16。
    *   训练时长：总计约 **7 小时**（共 6 个 Epoch）。
    *   推理延迟：总实时延迟为 **47.04ms**（相比基准增加约 7ms，但仍满足实时性要求）。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个城镇、3 种赛道上进行了广泛测试，所有结果均为 3 次独立运行的平均值。
*   **消融实验**：
    *   验证了动作解码器、扩散头、运动学状态等组件的贡献。
    *   探讨了隐藏层维度（从 128 到 4096）对性能的影响，证明了特征瓶颈的必要性。
    *   对比了横向动作与纵向动作条件的效果。
*   **充分性评价**：实验设计全面，不仅有宏观分数对比，还有针对碰撞类型、路线偏离的深入分析，客观地展示了模型在安全性和指令遵循方面的提升。

### 6. 主要结论与发现
*   **性能突破**：LAD-Drive 达到了 SOTA 水平，平均驾驶分数 (DS) 达到 **68.2**，比 LMDrive 基准提升了 **59%**。
*   **安全性提升**：显著降低了碰撞率，其中与车辆碰撞减少 76%，与行人碰撞减少 80%，路线偏离减少 81%。
*   **解耦优势**：证明了将“做什么”（意图）与“怎么做”（执行）解耦能有效减轻模型负担，避免导航决策错误。
*   **特征对齐**：发现直接使用高维 LLM 嵌入会引入噪声，通过低维瓶颈层过滤特征对扩散模型的稳定性至关重要。

### 7. 优点
*   **结构优雅**：通过概率分布而非硬编码动作来桥接语义与物理空间，更符合人类驾驶的决策逻辑。
*   **高效推理**：采用截断扩散和锚点初始化，仅需 2 步迭代，克服了传统扩散模型推理慢的弊端。
*   **鲁棒性强**：在长距离导航和复杂交互场景（如避让骑行者）中表现出极强的指令遵循能力。

### 8. 不足与局限
*   **环境局限**：实验主要在 CARLA 模拟器中完成，尚未在真实世界道路数据上进行验证。
*   **动作空间限制**：目前的动作解码器主要侧重于横向意图，纵向控制（如加减速）仍主要依赖运动学状态和空间约束，对复杂纵向语义（如“在某处缓慢停下”）的覆盖可能不足。
*   **闭环依赖**：模型性能高度依赖于底层 PID 控制器的调优。

（完）
