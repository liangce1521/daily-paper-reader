---
title: Improving Diffusion Planners by Self-Supervised Action Gating with Energies
title_zh: 通过基于能量的自监督动作门控改进扩散规划器
authors: "Yuan Lu, Dongqi Han, Yansen Wang, Dongsheng Li"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02650v1"
tags: ["query:eco-ai"]
score: 8.0
evidence: 用于轨迹规划离线强化学习的扩散规划器
tldr: 针对扩散规划器在离线强化学习中因价值引导导致轨迹与环境动力学不一致的问题，本文提出SAGE方法。SAGE通过自监督学习JEPA编码器和潜空间预测器，在推理阶段利用潜空间预测误差作为能量分数来惩罚不一致的计划。该方法无需环境交互或重新训练策略，显著提升了扩散规划器在多种任务中的性能和鲁棒性。
selection_source: fresh_fetch
motivation: 扩散规划器在推理时容易选择价值评分高但局部动力学不一致的脆弱轨迹。
method: 提出SAGE方法，利用JEPA架构在潜空间计算预测误差作为能量分数，对候选轨迹进行重新排序。
result: 在运动、导航和操作等基准测试中，SAGE在不增加额外训练负担的情况下显著提高了规划器的表现。
conclusion: SAGE通过引入潜空间一致性约束，有效解决了扩散规划中的动力学失真问题，增强了离线强化学习的可靠性。
---

## 摘要
扩散规划器是离线强化学习的一种强大方法，但当价值引导的选择倾向于那些得分高但在局部与环境动力学不一致的轨迹时，它们可能会失败，从而导致执行的脆弱性。我们提出了基于能量的自监督动作门控（SAGE），这是一种推理阶段的重排序方法，它利用潜在一致性信号来惩罚动力学不一致的规划。SAGE 在离线状态序列上训练一个联合嵌入预测架构（JEPA）编码器，并针对短时域转换训练一个以动作为条件的潜在预测器。在测试时，SAGE 根据潜在预测误差为每个采样候选分配一个能量值，并将该可行性得分与价值估计相结合来选择动作。SAGE 可以集成到现有的扩散规划流水线中，这些流水线能够采样轨迹并通过价值评分选择动作；它不需要环境回滚（rollouts），也不需要重新训练策略。在运动、导航和操作基准测试中，SAGE 提高了扩散规划器的性能和鲁棒性。

## Abstract
Diffusion planners are a strong approach for offline reinforcement learning, but they can fail when value-guided selection favours trajectories that score well yet are locally inconsistent with the environment dynamics, resulting in brittle execution. We propose Self-supervised Action Gating with Energies (SAGE), an inference-time re-ranking method that penalises dynamically inconsistent plans using a latent consistency signal. SAGE trains a Joint-Embedding Predictive Architecture (JEPA) encoder on offline state sequences and an action-conditioned latent predictor for short horizon transitions. At test time, SAGE assigns each sampled candidate an energy given by its latent prediction error and combines this feasibility score with value estimates to select actions. SAGE can integrate into existing diffusion planning pipelines that can sample trajectories and select actions via value scoring; it requires no environment rollouts and no policy re-training. Across locomotion, navigation, and manipulation benchmarks, SAGE improves the performance and robustness of diffusion planners.

---

## 论文详细总结（自动生成）

以下是对论文《Improving Diffusion Planners by Self-Supervised Action Gating with Energies》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：扩散规划器（Diffusion Planners）在离线强化学习中表现优异，但在推理阶段存在一个隐蔽的失效模式：**“价值引导的幻觉”**。即规划器倾向于选择那些预测价值（Value）极高、但在物理动力学上不可行（局部不一致）的轨迹。
*   **研究动机**：现有的方法往往将“可行性”与“价值”混为一谈。当智能体执行一个局部不可行的计划开头时，误差会迅速累积，导致长程任务失败。作者认为应将**可行性（Feasibility）**作为一个独立的信号，在推理阶段对候选轨迹进行重新过滤和排序。

### 2. 方法论
SAGE 提出了一种推理阶段的重排序机制，包含两个离线训练阶段和一个推理阶段：

*   **核心思想**：利用自监督学习构建一个“能量分数”，衡量候选轨迹的开头部分是否符合环境动力学。
*   **关键技术细节**：
    1.  **阶段 I：训练预测性状态表示（JEPA）**：使用联合嵌入预测架构（JEPA）在离线状态序列上训练编码器。通过掩码（Masking）预测未来的潜空间嵌入，并引入 VICReg 正则化防止表征崩溃。
    2.  **阶段 II：训练动作条件潜空间预测器**：在冻结的 JEPA 空间内，训练一个 Transformer 预测器 $f_\eta(z_t, a_t) \to \hat{z}_{t+1}$。损失函数结合了单步预测、短时域回滚（Rollout）以及一个防止模型忽略动作输入的“动作使用铰链损失（Action-usage hinge）”。
    3.  **推理阶段（SAGE Gating）**：
        *   从扩散模型采样 $C$ 条候选轨迹。
        *   计算每条轨迹前 $K$ 步的潜空间预测误差，定义为**能量 $E$**。
        *   **过滤与排序**：保留能量最低的 $P$ 比例候选者，然后在剩余候选中根据 $J(\tau) - \lambda E(\tau)$（价值减去能量惩罚）选择最优动作。

### 3. 实验设计
*   **数据集/场景**：使用了 **D4RL** 标准基准测试，涵盖：
    *   **运动（Locomotion）**：HalfCheetah, Hopper, Walker2d。
    *   **导航（Navigation）**：AntMaze, Maze2D。
    *   **操作（Manipulation）**：Franka Kitchen。
*   **Benchmark 与对比方法**：
    *   **传统离线 RL**：BC, BCQ, CQL, IQL。
    *   **扩散策略**：DQL, IDQL。
    *   **扩散规划器**：Diffuser, DV (当前 SOTA), 以及针对可靠性改进的 RGG, LoMAP, LDCQ。

### 4. 资源与算力
*   **硬件**：论文明确提到使用 **Nvidia A100 GPU** 进行训练和推理。
*   **训练时长**：JEPA 编码器和预测器各训练 200,000 步；扩散规划器训练 1,000,000 步。
*   **推理开销**：SAGE 增加的计算负担较轻，实测推理延迟仅比基准方法（DV）高出约 **6.8%**。

### 5. 实验数量与充分性
*   **实验规模**：在 18 个不同的 D4RL 任务上进行了测试。
*   **评估严谨性**：每个任务使用了 **500 个随机种子**进行评估（远高于行业标准的 10-50 个种子），并进行了统计显著性检验（Welch's t-test 和 Bootstrap 分布分析）。
*   **消融实验**：详细分析了前缀长度 $K$、保留率 $P$ 和惩罚权重 $\lambda$ 的影响，并对比了不同架构（如直接在状态空间预测）的效果。实验设计非常充分、客观且公平。

### 6. 主要结论与发现
*   **性能提升**：SAGE 在不改变原始扩散模型和价值函数的情况下，显著提升了所有任务的得分，尤其是在复杂的 Kitchen 和 AntMaze 任务中。
*   **可行性检测**：实验证明，SAGE 的能量分数能准确识别出被人工篡改的不可行轨迹（AUROC 接近 1.0）。
*   **解耦的有效性**：证明了将可行性作为独立门控信号比单纯依赖价值函数排序更具鲁棒性。

### 7. 优点
*   **模块化（Plug-and-play）**：作为推理插件，无需重新训练复杂的扩散模型或价值网络，易于集成到现有流水线。
*   **自监督驱动**：可行性信号的获取不需要奖励函数，仅依赖离线数据中的动力学一致性。
*   **高可靠性**：通过大规模种子评估证明了其在减少“脆弱执行”方面的卓越能力。

### 8. 不足与局限
*   **超参数敏感性**：虽然论文给出了一组通用参数，但在极端不同的环境中，前缀长度 $K$ 和惩罚权重 $\lambda$ 可能仍需微调。
*   **短视偏见**：SAGE 主要关注轨迹开头的可行性，虽然这解决了最紧迫的执行问题，但对于轨迹中后段可能出现的动力学漂移关注较少。
*   **依赖逆动力学**：如果基础规划器只生成状态，SAGE 需要依赖逆动力学模型来推断动作，这可能引入额外的建模误差。

（完）
