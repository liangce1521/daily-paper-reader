---
title: Real-Time Generative Policy via Langevin-Guided Flow Matching for Autonomous Driving
title_zh: 基于郎之万引导流匹配的自动驾驶实时生成式策略
authors: "Tianze Zhu, Yinuo Wang, Wenjun Zou, Tianyi Zhang, Likun Wang, Letian Tao, Feihong Zhang, Yao Lyu, Shengbo Eben Li"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02613v1"
tags: ["query:eco-ai"]
score: 9.0
evidence: 用于自动驾驶实时决策和控制的生成式策略
tldr: 针对自动驾驶中生成式强化学习策略推理延迟高的问题，本文提出DACER-F算法。该方法将流匹配（Flow Matching）引入在线强化学习，利用Langevin动力学和Q函数梯度优化目标分布，使策略能在单步推理中生成高质量动作。实验证明，DACER-F在复杂驾驶场景和DMC基准测试中均优于现有方法，且具备极低的推理延迟，兼顾了探索能力与实时性。
selection_source: fresh_fetch
motivation: 生成式策略虽能建模复杂分布以增强探索，但其固有的高推理延迟限制了在实时自动驾驶决策中的应用。
method: 提出DACER-F，通过流匹配实现单步推理，并利用Langevin动力学结合Q函数梯度动态优化经验回放中的动作分布。
result: 在复杂驾驶仿真和DMC基准测试中，DACER-F在保持超低延迟的同时，性能显著优于DACER和DSAC等基准算法。
conclusion: DACER-F证明了流匹配在强化学习中的高效性，为实时、高性能的生成式决策控制提供了新的解决方案。
---

## 摘要
强化学习 (RL) 是自动驾驶系统中的一种基础方法，其中生成式策略通过利用其建模复杂分布的能力来增强探索，展现出巨大的潜力。然而，其固有的高推理延迟严重阻碍了它们在实时决策和控制中的部署。为了解决这一问题，我们通过将流匹配引入在线强化学习，提出了基于流匹配的带熵调节器的扩散 Actor-Critic (DACER-F)，从而能够在单次推理步骤中生成具有竞争力的动作。通过利用郎之万动力学和 Q 函数的梯度，DACER-F 动态地将经验回放中的动作优化至一个目标分布，该分布平衡了高 Q 值信息与探索行为。随后，训练流策略以高效地学习从简单先验分布到该动态目标的映射。在复杂的多车道和交叉路口仿真中，DACER-F 的表现优于基准方法——带熵调节器的扩散 Actor-Critic (DACER) 和分布软 Actor-Critic (DSAC)，同时保持了极低的推理延迟。DACER-F 进一步在标准强化学习基准 DeepMind Control Suite (DMC) 上展示了其可扩展性，在 humanoid-stand 任务中获得了 775.8 分，超越了以往的方法。总而言之，这些结果确立了 DACER-F 作为一种高性能且计算高效的强化学习算法。

## Abstract
Reinforcement learning (RL) is a fundamental methodology in autonomous driving systems, where generative policies exhibit considerable potential by leveraging their ability to model complex distributions to enhance exploration. However, their inherent high inference latency severely impedes their deployment in real-time decision-making and control. To address this issue, we propose diffusion actor-critic with entropy regulator via flow matching (DACER-F) by introducing flow matching into online RL, enabling the generation of competitive actions in a single inference step. By leveraging Langevin dynamics and gradients of the Q-function, DACER-F dynamically optimizes actions from experience replay toward a target distribution that balances high Q-value information with exploratory behavior. The flow policy is then trained to efficiently learn a mapping from a simple prior distribution to this dynamic target. In complex multi-lane and intersection simulations, DACER-F outperforms baselines diffusion actor-critic with entropy regulator (DACER) and distributional soft actor-critic (DSAC), while maintaining an ultra-low inference latency. DACER-F further demonstrates its scalability on standard RL benchmark DeepMind Control Suite (DMC), achieving a score of 775.8 in the humanoid-stand task and surpassing prior methods. Collectively, these results establish DACER-F as a high-performance and computationally efficient RL algorithm.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DACER-F** 的新型强化学习算法，旨在解决生成式策略在自动驾驶实时应用中的高延迟问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在自动驾驶中，强化学习（RL）需要处理复杂的、多峰值的动作分布。生成式模型（如扩散模型）虽能提供强大的表达能力和探索能力，但其多步迭代采样导致的**高推理延迟**使其难以满足实时控制（通常要求毫秒级响应）的需求。
*   **核心问题**：如何在保持生成式策略强大表达能力的同时，实现单步推理（低延迟），并解决在线强化学习中缺乏固定目标分布（Target Distribution）来训练流匹配模型的问题。

### 2. 方法论
*   **核心思想**：将**流匹配（Flow Matching）**引入在线强化学习，并利用**郎之万动力学（Langevin Dynamics）**结合 Q 函数梯度来动态引导策略学习。
*   **关键技术细节**：
    *   **流匹配策略表示**：不同于扩散模型的随机微分方程（SDE），流匹配采用确定性常微分方程（ODE），将噪声分布映射到动作分布。通过单步欧拉法即可生成动作，大幅降低延迟。
    *   **动态目标引导机制**：将最优策略建模为基于 Q 函数的能量分布 $p(a|s) \propto \exp(Q(s, a)/\alpha)$。
    *   **郎之万优化动作**：从经验回放池中提取动作，利用 Q 函数的梯度进行郎之万采样，生成既能获得高 Q 值又具备探索性的“目标动作” $a^*$。
    *   **损失函数设计**：采用混合目标函数，结合了**策略梯度项**（直接最大化 Q 值）和**流匹配模仿项**（使策略输出靠近郎之万优化后的目标动作 $a^*$）。

### 3. 实验设计
*   **实验场景**：
    *   **自动驾驶仿真**：基于程序化生成的场景，包括多车道高速公路（换道、汇入）和城市交叉路口（左转、直行、掉头等）。设置了稀疏、普通、稠密三种交通流密度。
    *   **通用 RL 基准**：DeepMind Control Suite (DMC) 中的 6 个高维连续控制任务（如 Humanoid 和 Dog 机器人）。
*   **对比基准（Benchmarks）**：
    *   **DSAC**：基于高斯分布的分布软 Actor-Critic 算法。
    *   **DACER**：基于扩散模型的在线强化学习算法（本文的前作/基准）。
    *   **其他**：SAC, DIPO, QSM, SDAC, QVPO 等多种先进的生成式或标准 RL 算法。

### 4. 资源与算力
*   **硬件配置**：实验使用了 **Intel Core i9-12900K CPU** 和 **NVIDIA RTX 3090 Ti GPU**。
*   **训练规模**：自动驾驶场景训练迭代次数为 $5 \times 10^5$ 次；DMC 任务迭代次数为 $1 \times 10^6$ 次。
*   **软件框架**：使用 GOPS（通用最优控制问题求解器）软件进行开发。

### 5. 实验数量与充分性
*   **实验规模**：针对驾驶场景和 DMC 任务分别进行了多组实验，每组实验均采用 **5 个随机种子**以保证结果的统计显著性，并给出了 95% 的置信区间。
*   **评估指标**：包括总平均回报（TAR）、到达率、碰撞率、推理时间、训练迭代时间等。
*   **充分性评价**：实验设计较为充分，不仅涵盖了特定领域的自动驾驶任务，还扩展到了高维度的机器人控制任务，验证了算法的通用性和扩展性。对比方法涵盖了从传统高斯策略到最新扩散策略的多种主流模型，对比公平且客观。

### 6. 主要结论与发现
*   **性能卓越**：在驾驶场景中，DACER-F 的最终平均回报比 DACER 高出 28.0%，比 DSAC 高出 34.0%。
*   **极低延迟**：DACER-F 的推理时间仅为 **0.28ms**，比扩散模型（DACER，1.75ms）缩短了 **84%**，与简单的 MLP 网络（DSAC，0.22ms）处于同一数量级，完全满足实时控制需求。
*   **高维任务突破**：在极具挑战性的 Humanoid-stand 任务中，DACER-F 取得了 775.8 的高分，而许多基准算法（如 SAC、DACER）在该任务上几乎无法学习。

### 7. 优点
*   **高效性**：成功将生成式模型的推理速度提升至实时级别，解决了生成式策略落地的最大痛点。
*   **稳定性**：引入郎之万动力学作为引导，比单纯的动作梯度上升（Action Gradient）更具鲁棒性，能更好地平衡开发（Exploitation）与探索（Exploration）。
*   **通用性**：算法不局限于驾驶任务，在复杂的足式机器人控制上也表现出极强的竞争力。

### 8. 不足与局限
*   **计算开销**：虽然推理极快，但在训练阶段需要计算 Q 函数关于动作的梯度并进行多步郎之万采样，训练时间成本高于传统的高斯策略（如 SAC）。
*   **超参数敏感性**：郎之万采样的步长 $\eta_a$ 和温度参数 $\alpha$ 可能需要针对不同任务进行精细调优。
*   **仿真与现实差距**：实验全部在仿真环境中完成，尚未在真实车辆硬件上进行实车验证，需考虑 Sim-to-Real 的迁移问题。

（完）
