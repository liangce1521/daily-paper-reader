---
title: Constraints Matrix Diffusion based Generative Neural Solver for Vehicle Routing Problems
title_zh: 基于约束矩阵扩散的车辆路径问题生成式神经求解器
authors: "Zhenwei Wang, Tiehua Zhang, Ning Xue, Ender Ozcan, Ling Wang, Ruibin Bai"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07568v1"
tags: ["query:eco-ai"]
score: 8.0
evidence: 用于车辆路径规划的生成式人工智能与强化学习
tldr: 针对车辆路径问题（VRP），现有自回归神经求解器在处理异构分布、相似节点表示及长决策链时鲁棒性不足。本文提出一种新型融合神经网络框架，利用离散噪声图扩散模型学习底层约束并生成约束分配矩阵。该矩阵作为图结构掩码自适应地集成到自回归求解器的特征学习与决策过程中，实现了全局视野与局部特征的有效结合。在CVRPlib数据集上的广泛实验证明，该模型在多种基准测试中达到了SOTA性能，显著提升了神经求解器的泛化能力。
selection_source: fresh_fetch
motivation: 旨在解决现有神经求解器在面对异构分布、相似节点特征及长决策周期时表现出的鲁棒性差和性能退化问题。
method: 提出一种融合框架，利用离散图扩散模型生成约束分配矩阵，并将其作为掩码嵌入自回归求解器的特征表示与决策流程中。
result: 在CVRPlib公用数据集的378个组合空间维度上进行了全面测试，实验结果表明该模型在多个基准任务中均取得了当前最优性能。
conclusion: 通过将约束感知的扩散模型与自回归架构相结合，能够有效捕捉并利用问题约束，显著增强神经求解器解决复杂VRP问题的效能。
---

## 摘要
在过去十年中，由生成式人工智能驱动的神经网络求解器因其卓越的计算效率和优异的推理能力，在车辆路径问题（VRPs）领域引起了广泛关注。特别是，结合了强化学习的自回归求解器已成为一种显著趋势。然而，现有的大部分工作强调神经方法的大规模泛化，而忽略了基于注意力机制的方法在问题参数异构分布下的有限鲁棒性。它们相对于启发式搜索的改进在很大程度上仍局限于人工策划的、固定分布的基准测试。此外，当节点表示高度相似或任务涉及长决策周期时，这些架构的性能往往会显著下降。为了解决上述局限性，我们提出了一种新型融合神经网络框架，该框架采用离散噪声图扩散模型来学习车辆路径问题的潜在约束，并生成约束分配矩阵。该矩阵随后被自适应地集成到自回归求解器的特征表示学习和决策过程中，作为图结构掩码，促进形成具有全局视野和局部特征集成特征的解。据我们所知，这项工作代表了在 CVRPlib 公共数据集内，跨越四个不同维度的 378 个组合空间对神经网络模型求解器进行的首次全面实验研究。广泛的实验评估表明，我们提出的融合模型有效地捕捉并利用了问题约束，在多个基准数据集上实现了最先进的性能。

## Abstract
Over the past decade, neural network solvers powered by generative artificial intelligence have garnered significant attention in the domain of vehicle routing problems (VRPs), owing to their exceptional computational efficiency and superior reasoning capabilities. In particular, autoregressive solvers integrated with reinforcement learning have emerged as a prominent trend. However, much of the existing work emphasizes large-scale generalization of neural approaches while neglecting the limited robustness of attention-based methods across heterogeneous distributions of problem parameters. Their improvements over heuristic search remain largely restricted to hand-curated, fixed-distribution benchmarks. Furthermore, these architectures tend to degrade significantly when node representations are highly similar or when tasks involve long decision horizons. To address the aforementioned limitations, we propose a novel fusion neural network framework that employs a discrete noise graph diffusion model to learn the underlying constraints of vehicle routing problems and generate a constraint assignment matrix. This matrix is subsequently integrated adaptively into the feature representation learning and decision process of the autoregressive solver, serving as a graph structure mask that facilitates the formation of solutions characterized by both global vision and local feature integration. To the best of our knowledge, this work represents the first comprehensive experimental investigation of neural network model solvers across a 378-combinatorial space spanning four distinct dimensions within the CVRPlib public dataset. Extensive experimental evaluations demonstrate that our proposed fusion model effectively captures and leverages problem constraints, achieving state-of-the-art performance across multiple benchmark datasets.

---

## 论文详细总结（自动生成）

以下是对论文《Constraints Matrix Diffusion based Generative Neural Solver for Vehicle Routing Problems》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文旨在解决现有基于深度强化学习（RL）的自回归神经求解器在处理带容量限制的车辆路径问题（CVRP）时的三大局限性：
*   **鲁棒性不足**：在面对异构分布（非独立同分布数据）时，基于注意力机制的方法表现不稳定。
*   **特征过度平滑（Oversmoothing）**：当节点坐标或需求高度相似时，全连接的注意力架构难以区分节点，导致决策退化。
*   **约束感知弱**：现有模型多为贪婪的似然最大化选择器，缺乏对底层约束（如哪些节点应属于同一路径）的显式建模，导致在长决策链中误差累积。

### 2. 方法论：核心思想与关键技术
论文提出了一种**融合图扩散先验与自回归决策**的框架，核心流程如下：
*   **数据增强（Data Augmentation）**：通过几何对称变换（旋转、镜像）和需求扰动（路径内需求置换），构建“多对一”映射，增强模型对最优解结构的识别能力。
*   **离散图扩散模型（Discrete Graph Diffusion）**：
    *   **约束矩阵生成**：将最优解转化为 $N \times N$ 的二值矩阵（若节点 $i, j$ 在同一路径则为1）。
    *   **前向加噪与反向去噪**：采用离散伯努利噪声（Bernoulli noise）而非连续高斯噪声，通过各向异性图神经网络（GNN）学习从噪声中重建约束矩阵。
*   **掩码引导的编码器（Mask-Guided Encoder）**：将扩散模型预测的约束矩阵作为“拓扑掩码”，限制消息传递仅在潜在的同路径节点间进行，从而缓解特征平滑问题。
*   **双指针融合解码器（Dual-Pointer Decoder）**：
    *   **全局指针**：关注全图上下文。
    *   **局部指针**：基于约束矩阵掩码关注局部特征。
    *   两者融合生成最终的注意力分布，并引入“节省算法（Savings Algorithm）”作为启发式偏置引导搜索。

### 3. 实验设计
*   **数据集**：
    *   **合成数据**：随机生成的 CVRP20/50/100 实例。
    *   **公用基准库 CVRPLIB**：包括 A、B、P、E、M 等经典系列。
    *   **XML100 跨分布测试集**：包含 10,000 个实例，涵盖仓库分布、节点分布、需求分布、路径长度四个维度的 378 种组合。
*   **对比方法（Baselines）**：
    *   **传统求解器**：Gurobi（精确解）、LKH3、HGS（启发式）、OR-Tools。
    *   **神经求解器**：AM、POMO、LEHD（重解码器）、ReLD、E-GAT、GASE 等。

### 4. 资源与算力
*   **硬件环境**：使用了一台配备 **NVIDIA RTX A6000 (48 GB)** GPU 和 Intel Xeon Silver 4126 CPU 的服务器。
*   **训练细节**：
    *   扩散模型：在 50,000 个 HGS 标注的实例上训练 50 个 epoch。
    *   自回归策略网络：在 200,000 个无标注实例上通过强化学习训练 100 个 epoch。
    *   推理阶段：扩散模型采用 DDIM 加速，仅需 50 步推理。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模测试，特别是对 XML100 的 10,000 个实例进行了系统性评估，这是目前神经求解器领域最全面的跨分布实验之一。
*   **消融实验**：对比了离散噪声 vs 高斯噪声的效果，并分别验证了编码器掩码（ME）和局部注意力解码器（LA）的贡献。
*   **客观性**：通过 Gap（与最优解的差距）和标准差（稳定性）双重指标衡量，并展示了在不同分布下的性能热力图，实验设计较为客观、公平。

### 6 主要结论与发现
*   **性能领先**：在合成数据集和 100 节点以内的 CVRPLIB 实例上，该模型达到了当前自回归神经求解器的 SOTA 水平。
*   **缓解平滑**：扩散先验掩码有效解决了节点特征过度平滑问题，在节点聚集或需求均匀的复杂分布下，性能提升尤为显著（部分实例 Gap 降低超过 20%）。
*   **泛化瓶颈**：尽管在中小规模表现优异，但当规模超过 200 节点或路径极长时，自回归模型的误差累积问题依然存在，且扩散模型生成的约束矩阵准确度会下降。

### 7. 优点（亮点）
*   **创新性结合**：首次将离散图扩散模型生成的约束先验引入自回归 VRP 求解框架。
*   **鲁棒性增强**：通过数据增强和掩码机制，显著提升了模型在 OOD（分布外）数据上的表现。
*   **全面评估**：对 XML100 数据集的 378 种组合进行了详尽测试，填补了该领域大规模跨分布评估的空白。

### 8. 不足与局限
*   **可扩展性限制**：模型在处理超大规模（如 >200 节点）问题时，性能优势减弱，且计算开销随节点数增加。
*   **依赖高质量标签**：扩散模型的训练依赖于 HGS 等传统算法生成的“近最优”标签，这在某种程度上限制了模型超越现有启发式算法上限的能力。
*   **推理延迟**：虽然引入了加速技术，但相比纯自回归模型，增加了扩散模型推理的额外步骤。

（完）
