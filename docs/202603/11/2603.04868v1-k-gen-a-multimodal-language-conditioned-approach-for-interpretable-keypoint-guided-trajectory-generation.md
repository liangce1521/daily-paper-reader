---
title: "K-Gen: A Multimodal Language-Conditioned Approach for Interpretable Keypoint-Guided Trajectory Generation"
title_zh: K-Gen：一种用于可解释关键点引导轨迹生成的语言条件多模态方法
authors: "Mingxuan Mu, Guo Yang, Lei Chen, Ping Wu, Jianxun Cui"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04868v1"
tags: ["query:eco-ai"]
score: 8.0
evidence: 自动驾驶仿真的生成式轨迹生成
tldr: 针对自动驾驶仿真中轨迹生成缺乏视觉上下文和可解释性的问题，本文提出K-Gen框架。该框架利用多模态大语言模型（MLLM）整合栅格化BEV地图与文本描述，通过生成带有推理逻辑的关键点来引导轨迹生成，并结合T-DAPO强化学习算法进行微调。实验证明，K-Gen在WOMD和nuPlan数据集上表现优于现有基准，实现了更具解释性和准确性的轨迹预测。
selection_source: fresh_fetch
motivation: 现有的基于LLM的轨迹生成方法过度依赖结构化数据，难以捕捉场景中丰富的非结构化视觉上下文。
method: 提出一种多模态框架，通过MLLM生成包含意图推理的关键点，并利用T-DAPO算法和精细化模块将其转化为最终轨迹。
result: 在WOMD和nuPlan数据集上的实验结果显示，K-Gen在生成轨迹的准确性和多样性方面均优于现有主流方法。
conclusion: 结合多模态推理与关键点引导的生成方式，能有效提升自动驾驶场景下轨迹预测的可解释性与性能。
---

## 摘要
生成逼真且多样化的轨迹是自动驾驶仿真中的一个关键挑战。虽然大语言模型（LLMs）展现出潜力，但现有方法通常依赖于矢量化地图等结构化数据，无法捕捉场景中丰富的非结构化视觉上下文。为解决这一问题，我们提出了 K-Gen，这是一个可解释的关键点引导多模态框架，它利用多模态大语言模型（MLLMs）将栅格化的 BEV 地图输入与文本场景描述统一起来。K-Gen 并非直接预测完整轨迹，而是生成可解释的关键点以及反映智能体意图的推理过程，随后由细化模块将其细化为准确的轨迹。为了进一步增强关键点生成，我们应用了 T-DAPO，这是一种轨迹感知的强化微调算法。在 WOMD 和 nuPlan 上的实验表明，K-Gen 优于现有的基准模型，突显了将多模态推理与关键点引导轨迹生成相结合的有效性。

## Abstract
Generating realistic and diverse trajectories is a critical challenge in autonomous driving simulation. While Large Language Models (LLMs) show promise, existing methods often rely on structured data like vectorized maps, which fail to capture the rich, unstructured visual context of a scene. To address this, we propose K-Gen, an interpretable keypoint-guided multimodal framework that leverages Multimodal Large Language Models (MLLMs) to unify rasterized BEV map inputs with textual scene descriptions. Instead of directly predicting full trajectories, K-Gen generates interpretable keypoints along with reasoning that reflects agent intentions, which are subsequently refined into accurate trajectories by a refinement module. To further enhance keypoint generation, we apply T-DAPO, a trajectory-aware reinforcement fine-tuning algorithm. Experiments on WOMD and nuPlan demonstrate that K-Gen outperforms existing baselines, highlighting the effectiveness of combining multimodal reasoning with keypoint-guided trajectory generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《K-Gen: A Multimodal Language-Conditioned Approach for Interpretable Keypoint-Guided Trajectory Generation》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：在自动驾驶仿真中，生成真实且多样化的交通参与者轨迹至关重要。
*   **核心挑战**：
    *   **数据表征局限**：现有基于大语言模型（LLM）的方法多依赖矢量化地图（结构化数据），难以捕捉复杂的视觉上下文（如精细的路面标线、复杂的交叉口几何结构）。
    *   **控制力与可解释性不足**：直接生成完整轨迹往往缺乏物理一致性，且难以解释智能体行为背后的意图。
*   **研究目标**：提出一种多模态框架，通过整合栅格化鸟瞰图（BEV）和文本描述，利用“推理-关键点-轨迹细化”的路径，提升轨迹生成的准确性、可解释性和安全性。

### 2. 方法论
K-Gen 采用两阶段生成架构，结合了多模态大模型（MLLM）的推理能力和专门的轨迹细化模块。
*   **核心思想**：不让 MLLM 直接输出复杂的长序列轨迹坐标，而是让其先进行思维链（CoT）推理，输出反映意图的**稀疏关键点**，再由下游模块进行物理平滑。
*   **关键技术细节**：
    *   **多模态输入**：整合 BEV 栅格地图（图像）和场景描述（文本，包含智能体类型、位置、速度等）。
    *   **关键点提取**：利用 Douglas-Peucker 算法和速度变化阈值，从原始轨迹中提取保留运动特征的稀疏点。
    *   **T-DAPO 算法**：提出“轨迹感知解耦裁剪与动态采样策略优化”（Trajectory-aware Decoupled Clip and Dynamic Sampling Policy Optimization）。这是一种强化学习微调（RFT）算法，针对轨迹生成的连续空间进行了优化，通过奖励函数（准确性、CoT 长度、格式正确性）引导模型生成更高质量的关键点。
    *   **TrajRefiner 模块**：基于 Transformer 的残差细化模块，接收 MLLM 生成的关键点、历史轨迹和智能体状态，预测残差修正量，确保最终轨迹符合运动学约束。

### 3. 实验设计
*   **数据集**：Waymo Open Motion Dataset (WOMD) 和 nuPlan。
*   **Benchmark（基准指标）**：
    *   **mADE**（平均位移误差）：衡量轨迹整体准确度。
    *   **mFDE**（终点位移误差）：衡量预测终点的准确度。
    *   **SCR**（场景碰撞率）：衡量生成轨迹的安全性和物理合理性。
*   **对比方法**：
    *   **语言引导基准**：LCTGen, InteractTraj。
    *   **多模态模型基准**：InternVL 系列（2B, 4B, 8B）, Qwen2.5-VL, Qwen3-VL。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 GPU** 进行训练。
*   **训练细节**：
    *   SFT 阶段：全参数微调，学习率 4e-5。
    *   TrajRefiner 阶段：AdamW 优化器，学习率 3e-4，训练 100 个 epoch。
*   **推理效率**：在单张 A100 上，处理每个场景的平均时间为 **1.63 秒**，证明了该方法在仿真应用中的可行性。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流的大规模自动驾驶数据集上进行了验证，涵盖了多种复杂的城市驾驶场景（交叉口、汇入、弯道）。
*   **消融实验**：
    *   **训练流程消融**：对比了 Zero-shot、SFT、SFT+GRPO、SFT+DAPO 等不同训练阶段的效果。
    *   **模块消融**：验证了 TrajRefiner 中相对坐标编码（RCE）、运动学一致性损失（KCL）和终点损失（FPL）的贡献。
*   **充分性评价**：实验设计较为全面，不仅有定量指标对比，还通过注意力热图（Attention Heatmaps）进行了定性分析，客观地展示了模型如何“关注”交通冲突点。

### 6. 主要结论与发现
*   **性能领先**：K-Gen 在 WOMD 和 nuPlan 上的 mADE 和 SCR 指标均显著优于现有基准模型。
*   **安全性提升**：相比于直接预测轨迹，通过关键点引导和 TrajRefiner 修正，场景碰撞率（SCR）大幅降低，生成的轨迹更具物理可行性。
*   **推理的价值**：CoT 推理不仅提供了可解释性，还帮助模型在复杂交互（如无保护左转、高速汇入）中识别潜在风险。
*   **模型规模并非唯一因素**：K-Gen (8B) 的表现优于更大规模或更新的通用多模态模型（如 Qwen3-VL），证明了针对性微调和任务解耦的有效性。

### 7. 优点
*   **可解释性强**：通过 CoT 展现智能体的决策逻辑（如“车辆1可能会继续南行穿过交叉口”），增强了仿真过程的透明度。
*   **多模态融合**：成功将栅格地图的视觉细节与文本的语义信息结合，弥补了矢量化表征的细节丢失。
*   **任务解耦设计**：将复杂的轨迹生成分解为“战略意图（关键点）”和“战术执行（细化）”，降低了 LLM 处理高频连续坐标的压力。

### 8. 不足与局限
*   **数据依赖**：推理数据的生成依赖于闭源高性能模型（如 Claude 3.7），这可能增加数据准备的成本和复杂性。
*   **智能体数量限制**：实验中为了平衡计算效率，将每场景智能体数量限制在 8 个以内，在极度拥堵的超大规模场景下的表现尚待验证。
*   **实时性挑战**：虽然 1.63 秒的推理速度对于离线仿真足够，但对于需要极高帧率的闭环实时仿真系统，仍存在一定的计算延迟压力。

（完）
